<!-- ⚠️ TEMPLATE — the architecture pillar. `bin/init` copies it to the path you declare in
     `mente.config.yml` (`pillars.architecture`). IT IS BORN EMPTY: you fill it in as your
     project takes shape, not in one sitting.
     ⭐ This is the file the router and the project rules point at as "the architectural source
     of truth". If it is empty, the AI builds by inference — and sounds exactly as confident
     doing it. Fill it before that happens. -->
# {{project}} — Architecture · source of truth

**Status:** draft · **Type:** pillar · **Updated:** {{date}} · **Owner:** {{owner}}
**Scope:** ⚠️ INSTANCE document — it describes THIS project's architecture, not the engine's.

> ## ⛔ THIS IS THE GROUND EVERYTHING IS BUILT ON
> When this file and the code disagree, **one of the two is a bug** — and you have to decide
> which. This file does not describe what you *wish*; it describes what **is**, measured. A pillar
> that lies is worse than no pillar: it gives false confidence.

---

## Purpose

⬜ **In one or two lines: what is this project, architecturally?** Not the product pitch — the
technical shape. Example of the kind of answer (delete it): *"a containerised conversational bot,
multi-instance, with a rules core and one adapter per channel."*

---

## 1 · THE COMPONENTS — the pieces and what each one does

⬜ **List the system's large pieces and the responsibility of each.** A piece with no declared
responsibility is a piece someone will use for something it was not meant for. Express it however
serves you — a node graph, layers, services, modules; the system imposes no shape, it only asks
that the shape be declared.

| Piece | What it does | What it depends on |
|---|---|---|
| ⬜ … | ⬜ … | ⬜ … |

---

## 2 · THE INVARIANTS — what must NEVER break

⬜ **The truths that must hold no matter what.** These are the ones that, if broken, make the
system stop being what it is. Example of the kind (delete it): *"a message is never processed
twice", "state is always read from the database, never from memory."* Each one, where possible,
with how it is verified.

- ⬜ …
- ⬜ …

---

## 3 · THE LIMITS — what this system is NOT

⬜ **What is deliberately out of scope.** A declared limit stops someone from "extending" the
system where it should not go. Example of the kind: *"does not retain user data beyond 30 days",
"does not call external services without a retry queue."*

- ⬜ …
- ⬜ …

---

## 4 · THE LOAD-BEARING DECISIONS — and why

⬜ **The architectural decisions everything else hangs from, each with its reason.** ⭐ The reason
is what stops a future session from re-arguing them, or breaking one without knowing. If a
decision has a recorded ADR, link it.

| Decision | Why | ADR |
|---|---|---|
| ⬜ … | ⬜ … | ⬜ … |

---

## 5 · WHAT "HEALTHY" LOOKS LIKE — how you know the architecture still holds

⬜ **Which command, test or signal confirms the above is still true.** Consistent with the
system's own law: architecture that lives only in this document is followed 40-60%; architecture
a script verifies, 100%. Point here at whatever measures it.

- ⬜ …

---

Related: `mente.config.yml` (`pillars.architecture`, which points here) · `CLAUDE.md` (the router
that declares this pillar) · `Mente/rules/decisions/` (the ADRs behind §4).
