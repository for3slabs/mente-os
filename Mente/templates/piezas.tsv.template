# ══════════════════════════════════════════════════════════════════════════
# ⚠️ THIS IS THE TEMPLATE. `bin/init` copies it to `piezas.tsv` on install.
#    It ships WITH the engine's rows already filled in — unlike cuentas.tsv,
#    this file cannot be born empty: without the engine rows, check-structure
#    has nothing to verify and goes silent about every piece that disappears.
#
# ⭐ KEEP IT IN SYNC WITH THE LIVE FILE. Every piece added to the engine needs
#    its row here too, or a fresh clone installs a table that does not describe
#    what it actually received — and the gap is silent, which is the worst kind.
# ══════════════════════════════════════════════════════════════════════════
#
# piezas.tsv — the single source of truth for WHERE each key piece lives.
# Read by bin/check-structure; verified by bin/test-f0-f6.
#
# ⭐ Moving a piece costs ONE line here. Do not hardcode a location in prose —
#    point at this table. A piece written but not declared here is a piece that
#    check-structure goes SILENT about when it disappears.
#
# Format (TAB-separated):  pieza <TAB> ruta <TAB> governance <TAB> clase <TAB> que_es
#
#   governance — WHO decides over this piece (authority, never a person's name):
#     engine   the engine governs it · universal · travels identical to every clone
#     owner    whoever installs the instance governs it · their criterion · NEVER a name
#     shared   the engine defines the FORM, the owner fills the CONTENT
#
#   clase — HOW the piece is born:
#     canonical  written by hand · the source of truth
#     generated  produced by a script (bin/generate-*)
#     fossil     historical · kept for reference · not live
#
# ⛔ governance answers a DIFFERENT question than clase. Do not merge them.
# ⛔ No instance data in any row: no owner name, no real repo/project name in que_es.
#    A person's name was never a governance value — it was a leak. The value is
#    the ROLE ("owner"), never who currently holds it.
#
# ⭐ HOW TO TELL engine FROM owner — the test that settles it:
#    ask "would a different installation edit this file?"  No  → engine.
#    A file the owner READS but never rewrites is engine (base-rules.md, the
#    four architects): doctrine ships identical, and mislabelling it as owner
#    would hide it from any check that verifies engine files carry no instance
#    data. The frontier in mente.config.yml is the authority; this column must
#    agree with it.
#
# ⚠️ THE `owner` ROWS DO NOT EXIST ON A FRESH CLONE. check-structure reports
#    them red until you write each one. ⭐ That red is your install checklist,
#    not a fault — a check that turned green there would be lying.
#
# ⬜ ADD YOUR OWN PIECES at the end, under their own section comment. A piece
#    of yours that is not declared here is a piece the structure check cannot
#    protect: the day it disappears, nothing says so.
# ══════════════════════════════════════════════════════════════════════════
pieza	ruta	governance	clase	que_es
# ── engine · the validators — the motor, nobody edits per instance ──
bateria	bin/test-f0-f6	engine	canonical	the living proof — the only thing that matters is failed: 0
val-salud	bin/check-health	engine	canonical	is the system healthy
val-citas	bin/check-links	engine	canonical	every internal pointer resolves to something that exists
val-estructura	bin/check-structure	engine	canonical	the tree is the one this table declares
val-bloques	bin/check-blocks	engine	canonical	the blocks meet their contract
val-campanas	bin/check-campaigns	engine	canonical	the campaigns meet their contract, and their blocks declare them back
val-suficiencia	bin/check-sufficiency	engine	canonical	a block can be resumed from disk alone
val-clear	bin/check-clear-ready	engine	canonical	refuses a reset if anything would be lost
grada-bloque	bin/grade-block	engine	canonical	product or MVP — measured, never opinion
gen-indice	bin/generate-index	engine	canonical	produces INDEX.md and STATES.md from the tree
gen-metricas	bin/generate-metrics	engine	canonical	re-measures and republishes METRICS.md
instalador	bin/init	engine	canonical	generates the instance files from templates/
# ── engine · the gates + hooks ──
puerta-critica	hooks/gate-critical.py	engine	canonical	blocks a destructive change with no rollback, and an insufficient close
pre-edit	hooks/pre-edit-standards.py	engine	canonical	injects the block's standards before an edit
pre-commit	hooks/pre-commit.sh	engine	canonical	blocks a commit that violates the block's contract
session-start	hooks/session-start.sh	engine	canonical	health + structure + indexes at session start
# ── engine · the contracts + inheritance model ──
contrato-bloque	rules/contract-block.md	engine	canonical	which fields a block carries (§A-K)
contrato-campana	rules/contract-campaign.md	engine	canonical	which fields a campaign carries, and its lower ceiling
contrato-doc	rules/contract-document.md	engine	canonical	the shape every document in the system follows
contrato-adr	rules/contract-adr.md	engine	canonical	the shape of a recorded decision
herencia	rules/rule-inheritance.md	engine	canonical	the 3 levels and how rules add up
metodo-fases	rules/ESTANDAR_Metodo_Fases_F.md	engine	canonical	the phase method for large work
ciclo-bloque	rules/block-lifecycle.md	engine	canonical	how a block opens and closes
regla-cierre	rules/rule-session-close.md	engine	canonical	register the session before a reset
convencion-nombres	rules/NAMING_CONVENTION.md	engine	canonical	how pieces are named
# ── engine · the reference docs ──
capacidades	CAPABILITIES.md	engine	canonical	what may run and what must not be touched — engine vs instance
readme-motor	README.md	engine	canonical	the engine's front page — from clone to working
quickstart	QUICKSTART.md	engine	canonical	from clone to working in 3 steps — for someone new to the system
backlog-motor	docs/ENGINE-BACKLOG.md	engine	canonical	what the engine still owes itself — gaps with their closing condition
changelog	CHANGELOG.md	engine	canonical	the engine's change history
version	VERSION	engine	generated	the engine's version number
gitignore	.gitignore	engine	canonical	what does NOT travel — secrets, live config, per-machine state
# ── engine · the rule levels + the four architects ──
reglas-universal	base-rules.md	engine	canonical	level 1 — conduct in any project
reglas-proyecto	PROJECT-RULES.md	owner	canonical	level 2 — this project's own rules
enrutador	CLAUDE-MENTE-OS.md	engine	canonical	the router — imported with @Mente/CLAUDE-MENTE-OS.md from the user's own CLAUDE.md
voz	memory/principles/owner-0-voice.md	engine	canonical	how the system communicates
patrones-importados	memory/principles/imported-patterns.md	engine	canonical	absorbed failure patterns — knowledge, ⛔ never the owner's criterion
contrato-entrega	memory/principles/contract-delivery.md	engine	canonical	what a response must contain, and what shape it takes
arquitecto-docs	memory/principles/owner-1-docs.md	engine	canonical	the criterion for documentation
arquitecto-dev	memory/principles/owner-2-dev.md	engine	canonical	the criterion for development
exp-doc-planning	memory/principles/expertise/doc-planning.md	engine	canonical	the base criterion for planning — extensible per instance
exp-dev-frontend	memory/principles/expertise/dev-frontend.md	engine	canonical	the base criterion for interface work — extensible per instance
exp-dev-database	memory/principles/expertise/dev-database.md	engine	canonical	the base criterion for data work — extensible per instance
exp-dev-backend	memory/principles/expertise/dev-backend.md	engine	canonical	the base criterion for backend work — extensible per instance
arquitecto-val	memory/principles/owner-3-validation.md	engine	canonical	the criterion for validation
# ── owner · whoever installs governs these · ⚠️ they do NOT exist on a fresh
#    clone — check-structure reports them red until the owner writes each one.
#    That red is the install checklist, not a fault (see QUICKSTART §3).
cold-start	memory/RETOMAR.md	owner	canonical	where we left off — read first when resuming
pendientes	memory/PENDIENTES.md	owner	canonical	non-urgent findings that must not be lost
gate	connection/bridges/BRIDGES.md	owner	canonical	which other installations may be reached, and under what permission
config-instancia	mente.config.yml	owner	canonical	the instance declaration — gitignored, never travels
pilar-arquitectura	Cerebro/ARQUITECTURA.md	owner	canonical	the architectural source of truth — the owner fills it
# ── engine · the blueprints bin/init stamps ──
val-declarados	bin/check-declared	engine	canonical	every engine file has a row in this table — the disk-side check
tpl-enrutador	templates/CLAUDE.md.template	engine	canonical	the blueprint bin/init stamps into the project's CLAUDE.md
tpl-reglas	templates/PROJECT-RULES.md.template	engine	canonical	the blueprint for the project-level rules
tpl-config	templates/mente.config.yml.template	engine	canonical	the blueprint for the instance declaration
tpl-workspace	templates/WORKSPACE.md.template	engine	canonical	the blueprint for the machine map — says WHERE, never WHAT
tpl-arquitectura	templates/ARQUITECTURA.md.template	engine	canonical	the blueprint for the architecture pillar — born empty
tpl-cold-start	templates/RETOMAR.md.template	engine	canonical	the blueprint for the cold-start brief
tpl-pendientes	templates/PENDIENTES.md.template	engine	canonical	the blueprint for the pending-items list
tpl-puentes	templates/bridges.md.template	engine	canonical	the blueprint for the registry of other installations
tpl-piezas	templates/piezas.tsv.template	engine	canonical	the blueprint for this table — the only one shipping with content
# ── shared · engine gives the form, owner writes the content ──
piezas	piezas.tsv	shared	canonical	this table — where each key piece lives
registro-cuentas	cuentas.tsv	shared	canonical	which account governs each repo — ⛔ never a token
indice	docs/INDEX.md	shared	generated	every document — bin/generate-index
estados	docs/STATES.md	shared	generated	blocks + health — bin/generate-index
metricas	docs/METRICS.md	shared	generated	every live number — bin/generate-metrics
decisiones	docs/DECISIONS.md	shared	generated	every ADR — bin/generate-index
workspace	docs/WORKSPACE.md	owner	canonical	which repo is which on THIS machine — gitignored, never travels
